//4. Create a variable to store a temperature in Celsius. Convert it to Fahrenheit and display the result.

let c = 50;
let f = 0;
f = (c * (9 / 5)) + 32;
console. log("The value of the temperature in Fahrenheit is " + f);
